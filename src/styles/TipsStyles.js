import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    justifyContent: 'center',
    alignItems: 'center'
  },
  tip: {
    fontSize: 18,
    textAlign: 'center',
    marginBottom: 20
  },
  counter: {
    marginBottom: 10,
    fontWeight: 'bold'
  }
});

